# Large artifacts — download separately (see CONTRIBUTING.md)
models/
runtimes/
*.gguf
*.zip

# Benchmark run noise — we keep results/*.json, drop the rest (any experiment)
**/results/*.log
**/results/*.err.log
**/results/*.png
**/results/*_partial.json
**/results/_unused.json

# Local orchestration helpers (machine-specific paths)
/run_all.ps1
/run_reruns.ps1

# scratch (contact sheets, previews)
/_cs/

# Python
__pycache__/
*.pyc

.DS_Store
