# Community results — context-length sweep

One file per contributor, from `python scripts/make_submission.py --name <label>`.
Each bundles your `results.json` (under `runs.context`) plus hardware info.

See the repo [CONTRIBUTING.md](../../../../CONTRIBUTING.md) to add yours. All
submissions are licensed CC BY 4.0.
